# TODO

- [x] When I load the US constitution default, the page tighly fits my screen, with no scrolling. However, if I load a page with attribution and date: no longer the case. Adjust even with attribution and date, it tightly fits one page.

- [ ] Give the header and footer and light background color

## Load saved data

- [x] Save recent documents in browser storage (Google Doc URL, title, attribution, date; exclude pasted markdown)
- [x] Create a new page for "load" with toggle next to view, edit, create new
- [x] The load page shows the load modal; the create page just shows create, like before
- [x] Homepage shows "Create New" button leading to create page, and below that "Load recent" with list of recents (including the Constitution sample)
- [x] Clicking on "WordCloudy" in top-left header navigates to the home page and clears URL query parameters
